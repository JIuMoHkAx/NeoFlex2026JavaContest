# ⛱️ Vacation Pay Calculator Service
Разработано в рамках [Neoflex Java Contest 2026]

Простое Spring Boot приложение для расчета суммы отпускных выплат сотрудникам.
Сервис учитывает среднюю зарплату, количество дней отпуска и (опционально) производственный календарь.

## 🚀 Функциональность

- **Простой расчет**: Расчет отпускных на основе средней зарплаты и количества календарных дней.
- **Расчет с учетом праздников**: Интеллектуальный пропуск праздничных и выходных дней при указании даты начала отпуска.
- **Гибкий календарь**: Возможность настройки праздников и выходных через конфигурационный файл `yml`.
- **Валидация данных**: Встроенная проверка корректности входных параметров (зарплата > 0, дни > 0).

## 🛠️ Технологии

- **Java 11+**
- **Spring Boot 3** (Web, Validation)
- **Lombok**
- **Maven**

## 📋 API End Points

### Расчет отпускных
`GET /calculaсte`  - В строгом соответствии с ТЗ -> Технически выполняет псевдо-редирект на `GET /calculate`
`GET /calculate`   - Основной end point микросервиса

**Параметры запроса:**
- `averageSalary` (BigDecimal, > 0): Средняя зарплата сотрудника за год.
- `vacationDays` (Integer, > 0): Количество дней отпуска.
- `startDate` (ISO Date, optional): Дата начала отпуска. Если указана, сервис исключает праздники и выходные из оплаты.

**Пример запроса:**
`GET /calculacte?averageSalary=60000&amp;vacationDays=14&amp;startDate=2024-05-01`
`GET /calculate?averageSalary=60000&amp;vacationDays=14&amp;startDate=2024-05-01`

**Параметры:**
- `averageSalary` (BigDecimal, > 0) - средняя ЗП за 12мес
- `vacationDays` (Integer, ≥ 1) - дней отпуска
- `startDate` (LocalDate, optional) - начало отпуска YYYY-MM-DD

## Архитектура
    Controller → Service → WorkingCalendar (Strategy)
            ↓
    GlobalExceptionHandler

## ⚙️ Конфигурация календаря
**Вы можете настроить праздничные и нерабочие дни в application.yml:**

`calendar:`
    `holidays:` [2025-01-01, 2025-01-07 ...]
    `average-days-in-month:` 29.3  # ТК РФ
    `nonWorkingDays:` [] # Оставьте пустым для стандартных СБ/ВС

## ⚠️ Обработка ошибок
Сервис возвращает структурированные ошибки в случае некорректных данных:
- 400 Bad Request — если параметры не прошли валидацию (например, отрицательная зарплата).
- 500 Internal Server Error — при возникновении непредвиденных ошибок на сервере.

Особенности проекта
✅ BigDecimal для денег
✅ DI через конструктор
✅ Полная обработка ошибок (400/500)
✅ Логирование
✅ Валидация параметров